-=<[ SimpleTTS ]>=-
Your chat has a voice now.
SimpleTTS reads Twitch chat out loud using neural TTS voices — each chatter gets their own. No accounts, no tokens, no setup hell. Type a channel name, hit connect.
Download the exe and run it. Everything else creates itself.
